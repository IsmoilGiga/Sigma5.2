let ism = prompt("Iltimos, ismingizni kiriting:");
let spaceId = prompt("Iltimos, spaceId'ni kiriting:");
let parol = prompt("Iltimos, spacePassword'ni kiriting:");

let info = {
    ism: ism,
    id: spaceId,
    parol: parol
};

console.log(info);